# 服务器内部 RPC

这里保存 Gate、Node 和其他服务器组件之间的内部 gRPC 协议。

`wali add node <name>` 会创建：

```text
api/server/<name>/<name>.proto
internal/<name>/rpc.go
```

执行 `make proto` 后，Go 代码生成到 `internal/pb/<name>`，不会与可分发的 proto
源文件混放。生成的 Client 接收 Wali `rpc/client.Client`，并通过 `rpc/lx`
上下文选择负载均衡、指定节点或状态定位路由。

每个服务的强类型 Client 作为进程级全局变量保存在 `internal/rpcclient`，例如
`rpcclient.PlayerClient`、`rpcclient.RoomClient`。它们共用一个底层连接池和服务发现订阅。
