# 客户端协议

这里保存客户端与 Gate 之间的业务 Body protobuf。

目录可以按业务域划分，例如：

```text
api/client/chat/v1/chat.proto
api/client/player/v1/player.proto
```

客户端协议只定义消息，不定义 gRPC service。Packet 的 Route、Seq 等传输字段由
Wali 网络协议负责，不要在每个业务消息中重复声明。
